# Build artifacts (anchored to repo root so they don't match the cmd/semidx/ dir)
/semantic-indexer-poc
/semidx
/chatrag
dist/
# Embedded React admin assets (built by scripts/build-web.sh / npm run build).
!internal/webui/dist/
!internal/webui/dist/**
*.test
coverage.out
coverage.html
# Local coverage profiles produced during dev (anything ending in .out at root)
/*.out
/*_cov.out

# Secrets / local env
.env
.env.*
!.env.example

# Editor / tooling / agent session dirs
.idea/
.vscode/
.claude/
.agents/
.cursor/
.grok/
.opencode/
.specify/
.mcp.json
.mcp.json.bak-*
opencode.json
opencode.json.bak-*

# property-test regression seeds (transient)
testdata/rapid/

# Deepwork progress files (Orchestrator session state)
.slim/deepwork/
.scannerwork/ bin/ semidx.exe
.scannerwork/
semidx.exe
docs/auditoria-2026-07-06.md
.slim/worktrees/
.slim/worktrees.json
.slim/deepwork/
coverage*.out

# Local build output
/bin/

# Accidental binary commits (chatrag was scrubbed from history)
/chatrag
/chatrag.exe

# Local SQLite indexes
*.db
*.sqlite
*.sqlite3

# Homelab-only docs (kept on disk, never tracked for public publish)
docs/internal/
