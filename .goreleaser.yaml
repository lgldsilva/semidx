# GoReleaser — cross-platform release artifacts for semidx.
#
# Builds the CLI for linux/macOS/Windows on amd64+arm64, packages each as
# tar.gz (zip on Windows) with README + LICENSE, emits SHA-256 checksums and a
# grouped changelog, and publishes a GitHub release. Run by the `goreleaser`
# job in .github/workflows/release.yml on a `v*` tag.
#
# Validate locally without publishing:  goreleaser check  &&  goreleaser release --snapshot --clean
version: 2

project_name: semidx

before:
  hooks:
    - go mod download

builds:
  - id: semidx
    main: ./cmd/semidx
    binary: semidx
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .ShortCommit }}
      - -X main.date={{ .Date }}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]

  - id: chatrag
    main: ./cmd/chatrag
    binary: chatrag
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]

archives:
  - id: semidx
    formats: [tar.gz]
    # Windows users expect a zip.
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md
      - LICENSE

checksum:
  name_template: checksums.txt
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot-{{ .ShortCommit }}"

changelog:
  sort: asc
  use: git
  groups:
    - title: Features
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: Fixes
      regexp: '^.*?fix(\(.+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - '^Merge '

# GitHub release (public). install.sh/ps1 fetch from here.
release:
  github:
    owner: lgldsilva
    name: semidx
  draft: false
  prerelease: auto

# Package manager taps exist (lgldsilva/homebrew-tap, lgldsilva/scoop-bucket)
# and are seeded manually for now. Do NOT enable `brews:` / `scoops:` here
# until a repository secret `TAP_GITHUB_TOKEN` (classic PAT with `repo` on
# those taps) is configured in this repo — GoReleaser's default
# GITHUB_TOKEN cannot push to sibling repositories, and a missing token
# aborts the entire release (including the GHCR image push).
# Track: https://github.com/lgldsilva/semidx/issues/16
